version: 1.13.1
description: 'remove kill-port from the dev script and devDependencies'

steps:
    - files:
        package.json:
            action: edit-json
            changes:
                - update:
                    properties:
                        "scripts": {
                            "dev": "webpack serve --config webpack.dev.js"
                        }
                - remove:
                    path: ['devDependencies', 'kill-port']
